export const add = (a: number, b: number): number => a + b;

export const multiply = (a: number, b: number): number => a * b;

export const unusedFunction = () => console.log('Эта функция не в бандле');
